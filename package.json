{
  "name": "@flosrn/ax",
  "version": "0.6.5",
  "description": "Agent-experience tooling for MakerKit turbo projects: worktrees, debug-as, vendor ownership.",
  "type": "module",
  "license": "UNLICENSED",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flosrn/ax.git"
  },
  "bin": {
    "ax": "./bin/ax.mjs"
  },
  "//exports": "The public surface, deliberately small. `./dotenv` is here because a project's own scripts must read env files the way this package does: the launcher in one repo kept a second copy of that grammar, and the two disagreed twice. A shared reader is the only way that stops recurring. Everything else stays internal.",
  "exports": {
    ".": "./src/index.mjs",
    "./dotenv": "./src/dotenv.mjs",
    "./ax.schema.json": "./ax.schema.json",
    "./package.json": "./package.json"
  },
  "files": [
    "bin",
    "src",
    "assets",
    "ax.schema.json"
  ],
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=22"
  }
}
